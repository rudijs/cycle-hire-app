import { white } from 'material-ui/styles/colors';

const theme = {
    form: {
        fields: {
            color: white,
            marginTop: 30
        },
        hintStyle: {
            color: white
        },
        buttons: {
            signin: {
                marginRight: 5
            }
        }
    }
};

export default theme;