import {white} from "material-ui/styles/colors";

const theme = {
    paper: {
        padding: 20
    },
    raisedButton: {
        textColor: white,
        backgroundColor: "#283f89"
    }
};

export default theme;